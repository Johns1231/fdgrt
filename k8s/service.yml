apiVersion: v1
kind: Service
metadata:
  name: edfe
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "edfe"
    port: 8080
  selector:
    app: edfe
